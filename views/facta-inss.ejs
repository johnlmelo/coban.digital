<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Integração INSS</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container mt-5">
    <h1>Integração INSS</h1>
    <hr/>
    <div class="mt-4">
      <h3>Upload de lista de contatos:</h3>
      <div class="upload-area" id="uploadArea">
        <input type="file" id="fileInput" accept=".xls,.xlsx" style="display: none;">
        <p>Arraste e solte um arquivo aqui ou <a href="#" id="selectFile">clique para selecionar</a></p>
      </div>
      <button class="btn btn-primary mt-3" id="convertButton">Enviar</button>
    </div>

    <div class="mt-4">
      <h3>Campanhas Enviadas</h3>
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Título da Campanha</th>
            <th>Contatos Enviados</th>
            <th>Status</th>
            <th>Data de Envio</th>
          </tr>
        </thead>
        <tbody id="campaignList">
          <!-- Conteúdo da tabela será adicionado aqui -->
        </tbody>
      </table>
    </div>
  </div>

  <!-- Modal -->
  <div class="modal fade" id="dataModal" tabindex="-1" role="dialog" aria-labelledby="dataModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="dataModalLabel">Lista de Contatos</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <table class="table table-bordered" style="max-height: 500px;overflow: auto;">
            <thead>
              <tr>
                <th>Nome</th>
                <th>Telefone</th>
              </tr>
            </thead>
            <tbody id="contactList">
              <!-- Conteúdo da tabela será adicionado aqui -->
            </tbody>
          </table>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Fechar</button>
          <button type="button" class="btn btn-primary" id="sendButton">Enviar Campanha</button>
        </div>
      </div>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.16.9/xlsx.full.min.js"></script>
  <script src="/js/facta_inss.js"></script>
</body>
</html>
